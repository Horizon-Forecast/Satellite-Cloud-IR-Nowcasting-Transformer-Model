# Horizon Forecast Phase B — Requirements
# Install PyTorch with CUDA first:
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128

torch>=2.1
numpy>=2.0
pandas>=2.0
scipy>=1.10
scikit-image>=0.21
opencv-python>=4.10
tqdm>=4.65
matplotlib>=3.7
Pillow>=10.0
PyYAML>=6.0
requests>=2.28
wandb>=0.17
python-pptx>=0.6.23     # scripts/build_poster.py — A0 poster builder
pyinstaller>=6.0        # demo/build_demo_exe.bat — standalone .exe
zstandard>=0.22         # data/compress_*.py — archive compression for RunPod transfer

# ── Data acquisition & preprocessing (only needed to rebuild the dataset) ──
eumdac>=2.2             # data/download_seviri.py — EUMETSAT SEVIRI download
cdsapi>=0.6             # data/download_era5.py   — ERA5 download (Copernicus CDS)
netCDF4>=1.6            # read ERA5 NetCDF
xarray>=2023.1         # ERA5 regridding
rasterio>=1.3           # satellite / SRTM raster handling
pyproj>=3.4             # map projections (SEVIRI elevation overlay)
